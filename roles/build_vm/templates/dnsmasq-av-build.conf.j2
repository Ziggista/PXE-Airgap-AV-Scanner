port=0
log-dhcp
enable-tftp
tftp-root={{ tftp_root }}
dhcp-boot=pxelinux.0

# Replace the range and interface values for the actual PXE network.
# dhcp-range=192.168.50.100,192.168.50.200,12h
# interface=eth1
